// This function will read in the JSON data from prcs_LevelData and store it in the levelData variable.
function imbueLevelData() {
    ( { levelOn, levelName, playerStart, princess, walls } = levelData);
}